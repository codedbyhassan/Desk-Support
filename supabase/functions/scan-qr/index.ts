import {body,errorResponse,json,requireUser} from "../_shared.ts";
Deno.serve(async(req)=>{try{const{user,supabase}=await requireUser(req);const x=await body(req);const code=String(x.code??"");if(!code)throw Error("code is required");const{data:qr,error:qe}=await supabase.from("qr_codes").select("id,company_id,status,expires_at").eq("code",code).single();if(qe)throw qe;if(qr.status!=="active"||(qr.expires_at&&new Date(qr.expires_at)<new Date())){await supabase.from("qr_scan_logs").insert({qr_code_id:qr.id,user_id:user.id,result:"invalid",metadata:{reason:"inactive_or_expired"}});return json({ok:false,valid:false},400)}const{data:log,error}=await supabase.from("qr_scan_logs").insert({qr_code_id:qr.id,user_id:user.id,result:"valid",ip_address:null,user_agent:req.headers.get("user-agent"),latitude:x.latitude??null,longitude:x.longitude??null,metadata:x.metadata??{}}).select().single();if(error)throw error;return json({ok:true,valid:true,scan:log,company_id:qr.company_id});}catch(e){return errorResponse(e)}});
