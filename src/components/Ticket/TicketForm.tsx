import { useEffect, useRef, useState, type FormEvent } from 'react'
import { supabase } from '@/lib/supabase'
import { useAuth } from '@/lib/auth'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Textarea } from '@/components/ui/textarea'
import { useToast } from '@/hooks/use-toast'
import { AlertCircle, FilePlus2, ImagePlus, Send, Tag, Trash2 } from 'lucide-react'
import { Spinner } from '@/components/ui/spinner'
import type { TicketPriority } from '@/types/database'

interface Category { id:string; name:string; description:string|null }
interface TicketFormProps { onSubmit?: (ticketId:string)=>void }
const priorities:Array<{value:TicketPriority;label:string;description:string}>=[{value:'low',label:'Low',description:'Can wait'},{value:'medium',label:'Medium',description:'Normal request'},{value:'high',label:'High',description:'Needs attention'},{value:'urgent',label:'Urgent',description:'Critical issue'}]
const channels=[{value:'portal',label:'Portal'},{value:'email',label:'Email'},{value:'phone',label:'Phone'},{value:'chat',label:'Chat'},{value:'other',label:'Other'}] as const
const MAX_IMAGE_SIZE=5*1024*1024;const ACCEPTED_IMAGE_TYPES=['image/jpeg','image/png','image/webp','image/gif'];const ATTACHMENT_BUCKET='ticket-attachments'

export function TicketForm({onSubmit}:TicketFormProps){
 const {user}=useAuth();const {toast}=useToast();const fileInputRef=useRef<HTMLInputElement>(null);const [subject,setSubject]=useState('');const [description,setDescription]=useState('');const [priority,setPriority]=useState<TicketPriority>('medium');const [channel,setChannel]=useState<(typeof channels)[number]['value']>('portal');const [categoryId,setCategoryId]=useState('none');const [attachment,setAttachment]=useState<File|null>(null);const [attachmentPreview,setAttachmentPreview]=useState('');const [categories,setCategories]=useState<Category[]>([]);const [loadingCategories,setLoadingCategories]=useState(true);const [loading,setLoading]=useState(false)
 useEffect(()=>{if(!user?.company_id)return;const load=async()=>{setLoadingCategories(true);try{const {data,error}=await supabase.from('ticket_categories').select('id,name,description').eq('company_id',user.company_id).eq('is_active',true).order('name');if(error)throw error;setCategories(data??[])}catch(error){toast({title:'Could not load categories',description:error instanceof Error?error.message:'You can still create a ticket.',variant:'destructive'})}finally{setLoadingCategories(false)}};void load()},[toast,user?.company_id])
 useEffect(()=>{if(!attachment){setAttachmentPreview('');return}const url=URL.createObjectURL(attachment);setAttachmentPreview(url);return()=>URL.revokeObjectURL(url)},[attachment])
 const handleFile=(file?:File)=>{if(!file)return;if(!ACCEPTED_IMAGE_TYPES.includes(file.type)){toast({title:'Unsupported image',description:'Use JPG, PNG, WEBP, or GIF.',variant:'destructive'});return}if(file.size>MAX_IMAGE_SIZE){toast({title:'Image is too large',description:'Attachments must be 5 MB or smaller.',variant:'destructive'});return}setAttachment(file)}
 const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();if(!user?.id||!user.company_id){toast({title:'Not authenticated',description:'Please sign in again.',variant:'destructive'});return}const cleanSubject=subject.trim();if(!cleanSubject){toast({title:'Subject is required',description:'Enter a short subject.',variant:'destructive'});return}setLoading(true);let uploadedPath:string|null=null;try{const {data,error}=await supabase.from('tickets').insert({company_id:user.company_id,subject:cleanSubject,description:description.trim()||null,priority,channel,category_id:categoryId==='none'?null:categoryId,requester_id:user.id,created_by:user.id}).select('id,ticket_number,status').single();if(error)throw error;if(attachment){const ext=attachment.name.split('.').pop()?.toLowerCase()||'jpg';uploadedPath=`${user.id}/${data.id}/${crypto.randomUUID()}.${ext}`;const {error:uploadError}=await supabase.storage.from(ATTACHMENT_BUCKET).upload(uploadedPath,attachment,{contentType:attachment.type,cacheControl:'3600',upsert:false});if(uploadError)throw uploadError;const {error:attachmentError}=await supabase.from('ticket_attachments').insert({ticket_id:data.id,uploaded_by:user.id,storage_path:uploadedPath,file_name:attachment.name,mime_type:attachment.type,file_size_bytes:attachment.size});if(attachmentError)throw attachmentError}const {error:historyError}=await supabase.from('ticket_status_history').insert({ticket_id:data.id,from_status:null,to_status:data.status,changed_by:user.id,note:null});if(historyError)throw historyError;toast({title:'Ticket created',description:`Ticket #${data.ticket_number} was created successfully.`});onSubmit?.(data.id);setSubject('');setDescription('');setPriority('medium');setChannel('portal');setCategoryId('none');setAttachment(null);if(fileInputRef.current)fileInputRef.current.value=''}catch(error){if(uploadedPath)await supabase.storage.from(ATTACHMENT_BUCKET).remove([uploadedPath]).catch(()=>undefined);toast({title:'Could not create ticket',description:error instanceof Error?error.message:'Please try again.',variant:'destructive'})}finally{setLoading(false)}}
 return <form onSubmit={submit} className="flex max-h-[calc(100vh-8rem)] flex-col overflow-hidden bg-card"><div className="shrink-0 border-b border-border bg-card px-5 py-5 pr-14 sm:px-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary"><FilePlus2 className="h-5 w-5"/></div><div><h2 className="text-lg font-semibold tracking-tight">Create a ticket</h2><p className="mt-1 text-sm text-muted-foreground">Describe the request and add an image when useful.</p></div></div></div><div className="min-h-0 flex-1 overflow-y-auto bg-card p-5 sm:p-6"><div className="space-y-7"><section className="space-y-4"><div><h3 className="text-sm font-semibold">Request details</h3><p className="mt-1 text-sm text-muted-foreground">Give the service team enough information to act.</p></div><div className="space-y-2"><Label htmlFor="ticket-subject">Subject <span className="text-destructive">*</span></Label><Input id="ticket-subject" value={subject} onChange={e=>setSubject(e.target.value)} maxLength={200} placeholder="e.g. Laptop cannot connect to Wi-Fi" required disabled={loading}/></div><div className="space-y-2"><Label htmlFor="ticket-description">Description</Label><Textarea id="ticket-description" value={description} onChange={e=>setDescription(e.target.value)} rows={6} placeholder="Explain what happened and any useful details." disabled={loading}/></div></section><section className="space-y-4 border-t border-border pt-6"><div><h3 className="text-sm font-semibold">Classification</h3><p className="mt-1 text-sm text-muted-foreground">Category can automatically route the ticket to the right department and team.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="space-y-2"><Label>Priority</Label><Select value={priority} onValueChange={v=>setPriority(v as TicketPriority)} disabled={loading}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{priorities.map(p=><SelectItem key={p.value} value={p.value}>{p.label} — {p.description}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label>Channel</Label><Select value={channel} onValueChange={v=>setChannel(v as typeof channel)} disabled={loading}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{channels.map(c=><SelectItem key={c.value} value={c.value}>{c.label}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label>Category</Label><Select value={categoryId} onValueChange={setCategoryId} disabled={loading||loadingCategories}><SelectTrigger><Tag className="mr-2 h-4 w-4 text-muted-foreground"/><SelectValue placeholder={loadingCategories?'Loading categories…':'No category'}/></SelectTrigger><SelectContent><SelectItem value="none">No category</SelectItem>{categories.map(c=><SelectItem key={c.id} value={c.id}>{c.name}</SelectItem>)}</SelectContent></Select></div></div></section><section className="space-y-4 border-t border-border pt-6"><div><h3 className="text-sm font-semibold">Image attachment</h3><p className="mt-1 text-sm text-muted-foreground">Stored privately in ticket storage. JPG, PNG, WEBP or GIF, up to 5 MB.</p></div>{attachment?<div className="overflow-hidden rounded-xl border border-border bg-muted/30">{attachmentPreview&&<img src={attachmentPreview} alt="Attachment preview" className="max-h-56 w-full object-contain bg-muted"/>}<div className="flex items-center justify-between gap-3 border-t border-border px-4 py-3"><div className="min-w-0"><p className="truncate text-sm font-medium">{attachment.name}</p><p className="text-xs text-muted-foreground">{(attachment.size/1024/1024).toFixed(2)} MB</p></div><Button type="button" variant="outline" size="sm" onClick={()=>{setAttachment(null);if(fileInputRef.current)fileInputRef.current.value=''}} disabled={loading}><Trash2 className="mr-2 h-4 w-4"/>Remove</Button></div></div>:<button type="button" onClick={()=>fileInputRef.current?.click()} disabled={loading} className="flex w-full flex-col items-center justify-center rounded-xl border border-dashed border-border bg-muted/30 px-6 py-8 text-center hover:bg-muted"><span className="mb-3 flex h-11 w-11 items-center justify-center rounded-xl border border-border bg-card text-primary shadow-sm"><ImagePlus className="h-5 w-5"/></span><span className="text-sm font-semibold">Choose an image</span><span className="mt-1 text-xs text-muted-foreground">JPG, PNG, WEBP or GIF · up to 5 MB</span></button>}<input ref={fileInputRef} type="file" accept={ACCEPTED_IMAGE_TYPES.join(',')} className="sr-only" onChange={e=>handleFile(e.target.files?.[0])} disabled={loading}/></section><div className="flex items-start gap-3 rounded-xl border border-border bg-muted p-4 text-sm text-muted-foreground"><AlertCircle className="mt-0.5 h-4 w-4 shrink-0"/><p>The ticket starts as <strong className="text-foreground">Open</strong>. Routing is applied from the selected category when configured.</p></div></div></div><div className="flex shrink-0 justify-end border-t border-border bg-muted/30 px-5 py-4 sm:px-6"><Button type="submit" disabled={loading||!subject.trim()} className="h-11 rounded-xl px-5 font-semibold">{loading?<><Spinner size="sm" className="mr-2"/>Creating ticket…</>:<><Send className="mr-2 h-4 w-4"/>Create ticket</>}</Button></div></form>
}
