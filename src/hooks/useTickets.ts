import { useCallback, useEffect, useState } from 'react'
import { supabase } from '@/lib/supabase'
import { useAuth } from '@/lib/auth'
import { useToast } from '@/hooks/use-toast'
import { fetchSupabasePage } from '@/lib/dataAccess'
import type { TicketPriority, TicketStatus } from '@/types/database'
export interface TicketWithHistory{id:string;ticket_number:number;company_id:string;subject:string;title?:string;description:string|null;status:TicketStatus;priority:TicketPriority;channel:string;category_id:string|null;department_id:string|null;team_id:string|null;requester_id:string;created_by:string;accepted_by:string|null;accepted_at:string|null;resolved_at:string|null;closed_at:string|null;due_at:string|null;photo_url:string|null;archived_at:string|null;metadata:Record<string,unknown>;created_at:string;updated_at:string;assigned_to?:string|null;category?:{id:string;name:string}|null;creator?:{id:string;full_name:string}|null;requester?:{id:string;full_name:string}|null;assignee?:{id:string;full_name:string}|null;assignment?:{id:string;assignee_id:string;assigned_at:string;unassigned_at:string|null;department_id:string|null;team_id:string|null}|null;status_history?:Array<{id:string;from_status:TicketStatus|null;to_status:TicketStatus;changed_by:string|null;changed_at:string;note:string|null}>}
const TICKET_COLUMNS=`id,ticket_number,company_id,subject,description,status,priority,channel,category_id,department_id,team_id,requester_id,created_by,accepted_by,accepted_at,resolved_at,closed_at,due_at,photo_url,archived_at,metadata,created_at,updated_at,category:ticket_categories!tickets_category_id_fkey(id,name),creator:profiles!tickets_created_by_fkey(id,full_name),requester:profiles!tickets_requester_id_fkey(id,full_name),assignment:ticket_assignments!ticket_assignments_ticket_id_fkey(id,assignee_id,assigned_at,unassigned_at,department_id,team_id),status_history:ticket_status_history(id,from_status,to_status,changed_by,changed_at,note)`
function activeAssignment(value:unknown){if(!Array.isArray(value))return value??null;return value.find(item=>item&&typeof item==='object'&&!(item as {unassigned_at?:string|null}).unassigned_at)??null}
function normalizeTicket(row:Record<string,unknown>):TicketWithHistory{const assignment=activeAssignment(row.assignment) as TicketWithHistory['assignment'];return{...row,title:String(row.subject??''),assigned_to:assignment?.assignee_id??null,assignment,status_history:Array.isArray(row.status_history)?row.status_history as TicketWithHistory['status_history']:[],category:Array.isArray(row.category)?(row.category[0]??null) as TicketWithHistory['category']:row.category as TicketWithHistory['category'],creator:Array.isArray(row.creator)?(row.creator[0]??null) as TicketWithHistory['creator']:row.creator as TicketWithHistory['creator'],requester:Array.isArray(row.requester)?(row.requester[0]??null) as TicketWithHistory['requester']:row.requester as TicketWithHistory['requester'],metadata:row.metadata&&typeof row.metadata==='object'?row.metadata as Record<string,unknown>:{}} as TicketWithHistory}
export function useTickets(){const [tickets,setTickets]=useState<TicketWithHistory[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);const {user}=useAuth();const {toast}=useToast();const fetchTickets=useCallback(async(filters?:{assetId?:string;status?:string;assignedTo?:string})=>{if(!user?.company_id){setTickets([]);setLoading(false);return}try{setLoading(true);const result=await fetchSupabasePage<Record<string,unknown>>('tickets',0,{pageSize:250,columns:TICKET_COLUMNS,orderBy:'created_at',ascending:false,filter:q=>{let next=q.eq('company_id',user.company_id).is('archived_at',null);if(filters?.status)next=next.eq('status',filters.status);return next}});let rows=result.data;if(filters?.assignedTo){const {data:links,error}=await supabase.from('ticket_assignments').select('ticket_id').eq('assignee_id',filters.assignedTo).is('unassigned_at',null);if(error)throw error;const allowed=new Set((links??[]).map(link=>link.ticket_id));rows=rows.filter(row=>allowed.has(String(row.id)))}if(filters?.assetId){const {data:links,error}=await supabase.from('asset_tickets').select('ticket_id').eq('asset_id',filters.assetId);if(error)throw error;const allowed=new Set((links??[]).map(link=>link.ticket_id));rows=rows.filter(row=>allowed.has(String(row.id)))}setTickets(rows.map(normalizeTicket));setError(null)}catch(err){const message=err instanceof Error?err.message:'Failed to load tickets';setError(message);toast({title:'Error loading tickets',description:message,variant:'destructive'})}finally{setLoading(false)}},[toast,user?.company_id]);const updateTicketStatus=useCallback(async(ticketId:string,newStatus:TicketStatus,note?:string)=>{if(!user?.id||!user.company_id)throw new Error('User is not authenticated or has no company.');const {data:current,error:currentError}=await supabase.from('tickets').select('status,resolved_at,closed_at').eq('id',ticketId).eq('company_id',user.company_id).single();if(currentError)throw currentError;const now=new Date().toISOString();const patch:Record<string,unknown>={status:newStatus,updated_at:now};if(newStatus==='resolved'){patch.resolved_at=now;patch.closed_at=null}else if(newStatus==='closed'){patch.resolved_at=current.resolved_at??now;patch.closed_at=now}else{patch.resolved_at=null;patch.closed_at=null}const {error}=await supabase.from('tickets').update(patch).eq('id',ticketId).eq('company_id',user.company_id);if(error)throw error;const {error:historyError}=await supabase.from('ticket_status_history').insert({ticket_id:ticketId,from_status:current.status,to_status:newStatus,changed_by:user.id,note:note??null});if(historyError)throw historyError;await fetchTickets();toast({title:'Status updated',description:`Ticket status changed to ${newStatus.replace('_',' ')}.`})},[fetchTickets,toast,user?.company_id,user?.id]);const assignTicket=useCallback(async(ticketId:string,assigneeId:string,departmentId?:string|null,teamId?:string|null)=>{if(!user?.id||!user.company_id)throw new Error('User is not authenticated or has no company.');const {data:ticket,error:ticketLoadError}=await supabase.from('tickets').select('department_id,team_id').eq('id',ticketId).eq('company_id',user.company_id).single();if(ticketLoadError)throw ticketLoadError;const {error:closeError}=await supabase.from('ticket_assignments').update({unassigned_at:new Date().toISOString()}).eq('ticket_id',ticketId).is('unassigned_at',null);if(closeError)throw closeError;const {error:insertError}=await supabase.from('ticket_assignments').insert({ticket_id:ticketId,assignee_id:assigneeId,assigned_by:user.id,department_id:departmentId??ticket.department_id,team_id:teamId??ticket.team_id});if(insertError)throw insertError;const patch:Record<string,unknown>={status:'in_progress',updated_at:new Date().toISOString()};if(departmentId!==undefined)patch.department_id=departmentId;if(teamId!==undefined)patch.team_id=teamId;const {error:ticketError}=await supabase.from('tickets').update(patch).eq('id',ticketId).eq('company_id',user.company_id);if(ticketError)throw ticketError;await fetchTickets();toast({title:'Ticket assigned',description:'The ticket is now owned by the selected agent.'})},[fetchTickets,toast,user?.company_id,user?.id]);const acceptTicket=useCallback(async(ticketId:string)=>{if(!user?.id||!user.company_id)throw new Error('User is not authenticated or has no company.');const now=new Date().toISOString();const {error}=await supabase.from('tickets').update({status:'in_progress',accepted_by:user.id,accepted_at:now,updated_at:now}).eq('id',ticketId).eq('company_id',user.company_id);if(error)throw error;const {error:historyError}=await supabase.from('ticket_status_history').insert({ticket_id:ticketId,from_status:'open',to_status:'in_progress',changed_by:user.id,note:'Ticket accepted'});if(historyError)throw historyError;await fetchTickets();toast({title:'Ticket accepted',description:'You are now the accepting agent.'})},[fetchTickets,toast,user?.company_id,user?.id]);useEffect(()=>{if(!user?.company_id)return;void fetchTickets();const channel=supabase.channel(`tickets:${user.company_id}`).on('postgres_changes',{event:'*',schema:'public',table:'tickets',filter:`company_id=eq.${user.company_id}`},()=>void fetchTickets()).subscribe();return()=>{void supabase.removeChannel(channel)}},[fetchTickets,user?.company_id]);return{tickets,loading,error,fetchTickets,updateTicketStatus,assignTicket,acceptTicket}}
