import { useState, type FormEvent } from 'react'
import { useNavigate, useLocation } from 'react-router-dom'
import { useAuth } from '@/lib/auth'
import { useToast } from '@/hooks/use-toast'
import { ArrowRight, BarChart3, Building2, Check, Lock, Mail, User } from 'lucide-react'
interface LoginPageProps{defaultToSignUp?:boolean}
export default function LoginPage({defaultToSignUp=false}:LoginPageProps){
 const navigate=useNavigate();const location=useLocation();const{signIn,signUp}=useAuth();const{toast}=useToast();const[isSignUp,setIsSignUp]=useState(defaultToSignUp||location.pathname==='/signup');const[loading,setLoading]=useState(false);const[signupComplete,setSignupComplete]=useState(false);const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[fullName,setFullName]=useState('');const[companyName,setCompanyName]=useState('')
 const notify=(title:string,description?:string,variant?:'default'|'destructive')=>toast({title,description,variant})
 const handleSubmit=async(e:FormEvent)=>{e.preventDefault();const trimmedEmail=email.trim().toLowerCase();if(!trimmedEmail||!password){notify('Missing credentials','Please enter your email and password.','destructive');return}if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(trimmedEmail)){notify('Invalid email','Please enter a valid email address.','destructive');return}setLoading(true);try{if(isSignUp){const name=fullName.trim(),company=companyName.trim();if(!name){notify('Full name required','Please enter your full name.','destructive');return}if(!company){notify('Company required','Please enter your company name.','destructive');return}const result=await signUp(trimmedEmail,password,name,company);if(result.emailConfirmationRequired){setSignupComplete(true);notify('Check your email','We sent a confirmation link to your email.')}else navigate('/app/dashboard',{replace:true})}else{await signIn(trimmedEmail,password);notify('Welcome back','You are now signed in.');setTimeout(()=>navigate('/app/dashboard',{replace:true}),100)}}catch(error:unknown){console.error('Auth error:',error);const message=error instanceof Error?error.message:'';let friendly=isSignUp?'Failed to create account':'Failed to login';if(message.includes('Invalid login credentials'))friendly='Invalid email or password. Please check your credentials.';else if(message.includes('Email not confirmed'))friendly='Please confirm your email before logging in.';else if(message.toLowerCase().includes('already registered'))friendly='An account with this email already exists.';else if(message.toLowerCase().includes('timeout'))friendly='Connection timeout. Please try again.';else if(message.toLowerCase().includes('fetch'))friendly='Network error. Please check your connection.';else if(message)friendly=message;notify(isSignUp?'Sign-up failed':'Sign-in failed',friendly,'destructive')}finally{setLoading(false)}}
 const toggleMode=()=>{const next=!isSignUp;setIsSignUp(next);setSignupComplete(false);navigate(next?'/signup':'/login',{replace:true})}
 return <div className="auth-page"><div className="auth-shell"><main className="auth-main"><div className="auth-form-wrap"><div className="auth-brand-row"><div className="auth-mark"><BarChart3 size={20} strokeWidth={2.5}/></div><span className="auth-brand">Desk-Support</span></div>{signupComplete?<div className="auth-success"><div className="auth-success-icon"><Mail size={32}/></div><p className="auth-eyebrow">Almost there</p><h1 className="auth-title">Check your email.</h1><p className="auth-copy">We sent a confirmation link to <strong>{email}</strong>. Click the link to verify your account and you'll be taken straight to your Desk-Support dashboard.</p><button className="auth-secondary" type="button" onClick={()=>{setSignupComplete(false);setPassword('')}}><ArrowRight size={16}/> Use a different account</button></div>:<><p className="auth-eyebrow">Support operations</p><h1 className="auth-title">{isSignUp?'Create your workspace.':'Welcome back.'}</h1><form className="auth-form" onSubmit={handleSubmit} noValidate>{isSignUp&&<><div className="auth-field"><label className="auth-label" htmlFor="fullName">Full name</label><div className="auth-input-wrap"><User className="auth-input-icon" size={18}/><input id="fullName" className="auth-input" type="text" placeholder="Your name" value={fullName} onChange={e=>setFullName(e.target.value)} disabled={loading} autoComplete="name" autoFocus required/></div></div><div className="auth-field"><label className="auth-label" htmlFor="companyName">Company</label><div className="auth-input-wrap"><Building2 className="auth-input-icon" size={18}/><input id="companyName" className="auth-input" type="text" placeholder="Company name" value={companyName} onChange={e=>setCompanyName(e.target.value)} disabled={loading} autoComplete="organization" required/></div></div></>}<div className="auth-field"><label className="auth-label" htmlFor="email">Email address</label><div className="auth-input-wrap"><Mail className="auth-input-icon" size={18}/><input id="email" className="auth-input" type="email" placeholder="you@company.com" value={email} onChange={e=>setEmail(e.target.value)} disabled={loading} autoComplete="email" autoFocus={!isSignUp} required/></div></div><div className="auth-field"><div className="auth-password-row"><label className="auth-label" htmlFor="password">Password</label>{!isSignUp&&<button type="button" className="auth-link" onClick={()=>notify('Password reset','Password reset is not configured in this release. Use your administrator or Supabase Auth recovery flow.')}>Forgot password?</button>}</div><div className="auth-input-wrap"><Lock className="auth-input-icon" size={18}/><input id="password" className="auth-input" type="password" placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)} disabled={loading} autoComplete={isSignUp?'new-password':'current-password'} required/></div></div><button className="auth-submit" type="submit" disabled={loading}>{loading?<><span className="auth-spinner" aria-hidden="true"/>{isSignUp?'Creating workspace...':'Signing in...'}</>:<>{isSignUp?'Create workspace':'Sign in'}<ArrowRight size={17}/></>}</button></form><p className="auth-switch">{isSignUp?'Already have an account?':"Don't have an account?"}{' '}<button type="button" className="auth-link" onClick={toggleMode} disabled={loading}>{isSignUp?'Sign in':'Create one'}</button></p></>}</div></main><aside className="auth-side"><div className="auth-side-inner"><span className="auth-side-kicker">Desk-Support</span><h2 className="auth-panel-title">Keep every support request moving.</h2><p className="auth-panel-copy">A focused workspace for tickets, assets and the people responsible for keeping work running.</p><div className="auth-points"><div className="auth-point"><span className="auth-point-icon"><Check size={16}/></span><span>Centralize incoming support requests</span></div><div className="auth-point"><span className="auth-point-icon"><Check size={16}/></span><span>Track work from open to resolved</span></div><div className="auth-point"><span className="auth-point-icon"><Check size={16}/></span><span>Keep equipment and ownership organized</span></div></div></div></aside></div></div>
}
